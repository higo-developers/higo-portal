import React from 'react';

export default class FinishedOperationsGrid extends React.Component {
    render() {
        return (
            <React.Fragment>
                Finalizadas.
            </React.Fragment>
        );
    }
}